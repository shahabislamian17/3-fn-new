'use client';

// This is a simplified layout for the admin section, assuming authentication is handled at a higher level.
export default function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
